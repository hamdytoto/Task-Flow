# TaskFlow Pro

Static Stage 1 dashboard UI built with semantic HTML and Tailwind CSS.

## Setup

```bash
npm install
npm run build
```

Open `index.html` in a browser after building. Use `npm run watch` while editing Tailwind classes.

Stage 1 intentionally contains no application behavior. Filters, task actions, search, notifications, and the create-task modal are presentational UI only.
